import 'package:flutter/material.dart';

class ChannelScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('The Channels'),
      ),
      body: Center(
        child: Text('Channel'),
      ),
    );
  }
}
